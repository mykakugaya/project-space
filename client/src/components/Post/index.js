import React, {useState, useEffect} from "react";

function Post(props) {
    const [forum, setForum] = useState(""); //forum name
    const [body, setBody] = useState("");   //text input
    const [author, setAuthor] = useState(""); //user db

    //props passed down from CreatePostForm
    //create post with clickable forum name
    //add PostResponseForm to bottom
}

export default Post;